# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# VARIÁVEIS DE AMBIENTE PARA VERCEL
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
#
# IMPORTANTE: Este arquivo é apenas um EXEMPLO.
# NÃO COMMITE este arquivo com valores reais!
#
# Configure estas variáveis no painel do Vercel:
# Settings → Environment Variables
#
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# ┌─────────────────────────────────────────────────────────────────────────┐
# │ OBRIGATÓRIAS - Sem estas, a aplicação NÃO funcionará                    │
# └─────────────────────────────────────────────────────────────────────────┘

# Banco de dados PostgreSQL
# Opção 1: Vercel Postgres (recomendado)
#   - Crie em: Vercel Dashboard → Storage → Create Database → Postgres
#   - Será preenchido automaticamente
#
# Opção 2: Supabase (grátis)
#   - Acesse: https://supabase.com
#   - Crie projeto → Settings → Database → Connection String
#   - Formato: postgresql://postgres:[PASSWORD]@[HOST]:5432/postgres
#
# Opção 3: Neon (grátis)
#   - Acesse: https://neon.tech
#   - Crie projeto → Connection String
#   - Formato: postgresql://[USER]:[PASSWORD]@[HOST]/[DATABASE]
DATABASE_URL=postgresql://usuario:senha@host:5432/database

# Secret para assinar JWT (mínimo 32 caracteres)
# Gere um valor seguro em: https://generate-secret.vercel.app/32
# Ou use: openssl rand -base64 32
JWT_SECRET=seu_jwt_secret_aqui_minimo_32_caracteres_aleatorios

# Nome do cookie de autenticação
COOKIE_NAME=auth_token

# Ambiente de produção
NODE_ENV=production

# ┌─────────────────────────────────────────────────────────────────────────┐
# │ OPCIONAIS - Melhoram a experiência, mas não são obrigatórias            │
# └─────────────────────────────────────────────────────────────────────────┘

# Título da plataforma (aparece no navegador e interface)
VITE_APP_TITLE=Mentoria Mário Machado

# URL do logo da plataforma
VITE_APP_LOGO=https://seu-dominio.com/logo.png

# ┌─────────────────────────────────────────────────────────────────────────┐
# │ INSTRUÇÕES PARA CONFIGURAR NO VERCEL                                    │
# └─────────────────────────────────────────────────────────────────────────┘
#
# 1. Acesse seu projeto no Vercel
# 2. Vá em: Settings → Environment Variables
# 3. Para cada variável:
#    - Clique em "Add New"
#    - Nome: copie o nome da variável (ex: DATABASE_URL)
#    - Value: cole o valor real (NÃO use os valores de exemplo!)
#    - Environment: selecione "Production", "Preview" e "Development"
#    - Clique em "Save"
# 4. Após adicionar todas, faça redeploy:
#    - Vá em: Deployments
#    - Clique nos 3 pontos do último deploy
#    - Clique em "Redeploy"
#
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# ┌─────────────────────────────────────────────────────────────────────────┐
# │ APÓS O DEPLOY                                                            │
# └─────────────────────────────────────────────────────────────────────────┘
#
# 1. Execute as migrações do banco:
#    - Opção A: Via Vercel CLI
#      $ vercel env pull .env.local
#      $ pnpm db:push
#
#    - Opção B: Via painel do banco de dados
#      Execute o SQL das migrações manualmente
#
# 2. Crie o gestor principal:
#    Execute no banco de dados:
#
#    INSERT INTO gestores (nome, email, senha, createdAt, updatedAt) 
#    VALUES (
#      'Mário Machado',
#      'mentoriaenemmario@gmail.com',
#      '$2b$10$[HASH_BCRYPT_AQUI]',
#      NOW(),
#      NOW()
#    );
#
#    Para gerar o hash bcrypt da senha:
#    - Acesse: https://bcrypt-generator.com
#    - Digite a senha: Valentes23!
#    - Rounds: 10
#    - Copie o hash gerado
#
# 3. Teste o login:
#    - Acesse: https://seu-projeto.vercel.app/login/gestor
#    - Email: mentoriaenemmario@gmail.com
#    - Senha: Valentes23!
#
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
